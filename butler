(((or ((glob "_build")
       (glob "*#")
       (glob "*.swp")
       (glob "*~")))
  ignore)

 ((or ((glob "lib/*.ml{,i}")
       (glob "bin/*.ml{,i}")))
  (run (make)))

 ((or ((glob "lib/*.ml{,i}")
       (glob "bin/*.ml{,i}")
       (glob "test/*.ml{,i}")))
  (run (make check))))
